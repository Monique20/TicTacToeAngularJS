function PriorityQueue() {
    let self = this;
    self.Queue = function (gift, priority) {
        let giftList={gift,priority};
        console.log(length);
        let arr = [];
        for (let i = 0; i < giftList.length; i++) {
            arr[i] = giftList[i];
        }
        console.log(arr.length)
        return arr.length ;  
    }  
}